node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# API keys & config lokal — jangan pernah commit (nilai key bisa bocor)
config.json

# Artefak sinkronisasi (Resilio/SyncThing) — bukan bagian proyek
.sync/

# Folder penyimpanan novel lokal (hasil generate app, bukan source)
Novel_Library/
